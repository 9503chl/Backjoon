#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

void Bt(int Array[],int n, int m) {
	for (int i = 1; i <= n; i++) {
		int num = m-1;
		while (Array[num] > n) {
			for (int j=0; j < num; j++)
		}
	}
}
int main()
{
	cin.tie(NULL); cout.tie(NULL); ios_base::sync_with_stdio(0);
	int N, M; cin >> N >> M; int cnt[7] = { 1, };
	Bt(cnt,N, M);
}